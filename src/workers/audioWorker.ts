/**
 * Réservé au moteur audio temps réel / OfflineAudioContext (spec).
 * Le décodage des fichiers se fait dans `src/renderer/audio/decodeAudio.ts`
 * (Web Audio API n’est pas exposée dans les Dedicated Workers).
 */
export {}
