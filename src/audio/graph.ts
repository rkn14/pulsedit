/**
 * Construction du graphe Web Audio (preview / offline) — Phase 4–5.
 */
export type GraphBuildOptions = {
  sampleRate: number
  channelCount: number
}

export function describeGraphPlaceholder(_opts: GraphBuildOptions): string {
  return 'graph-placeholder'
}
