/**
 * Moteur audio — logique principale côté bundle (orchestration worker à brancher).
 * La lecture / graphe temps réel iront dans `src/workers/audioWorker.ts` si besoin.
 */
export const INTERNAL_SAMPLE_RATE = 44100
